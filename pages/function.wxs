var arrayfind = function (arr, string) {
    var flag=0;
    for (var i = 0; i < arr.length; i++) {
        if(arr[i]==string){
            flag=1;
            break;
        }
    }

    return flag;
}
module.exports.arrayfind = arrayfind ;